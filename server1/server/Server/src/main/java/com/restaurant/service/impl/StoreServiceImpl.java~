package com.restaurant.service.impl;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.restaurant.mapper.StoreMapper;
import com.restaurant.model.DishCategory;
import com.restaurant.model.Store;
import com.restaurant.service.StoreService;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class StoreServiceImpl extends ServiceImpl<StoreMapper, Store>
        implements StoreService {
    @Override
    public List<DishCategory> listCategories(Integer id) {

        // 帮我实现这个方法，这个方法应该返回给定商店id的菜品类别列表
        // 列表应该从数据库中使用StoreMapper获取
        // 如果商店id在数据库中找不到，则应该返回一个空列表
        // 帮我写出逻辑
        return this.baseMapper.listCategories(id);
    }
}