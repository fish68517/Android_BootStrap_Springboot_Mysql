<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduation.mapper.RecommendationsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.graduation.entity.Recommendations">
        <id column="recommendation_id" property="recommendationId" />
        <result column="admin_id" property="adminId" />
        <result column="user_id" property="userId" />
        <result column="game_id" property="gameId" />
        <result column="reason" property="reason" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        recommendation_id, admin_id, user_id, game_id, reason, created_at
    </sql>

</mapper>
